# Acceptance и smoke-проверка прогноза

## Smoke (технический)

1. Убедиться, что установлен Python (`py -3 --version` или `python --version`).
2. Выполнить:

```powershell
node scripts/smoke-forecast.js
```

Ожидаемо:
- вывод `[SMOKE][forecast] OK`;
- в JSON есть `model`, `horizon: 3`, блок `quality`.

## Acceptance-сценарии (UI/агент)

1. **Базовый прогноз затрат**
- Вопрос: `Спрогнозируй затраты ТОИР на 3 месяца`.
- Ожидаемо: текстовый ответ + график (`Факт`, `Прогноз`, интервалы) + таблица прогнозных точек.

2. **Прогноз с явным форматом**
- Вопрос: `Покажи прогноз затрат ТОИР на 6 месяцев в виде таблицы`.
- Ожидаемо: таблица с 6 строками, поля периода и прогноза, интервалы (если доступны).

3. **Прогноз по трудозатратам**
- Вопрос: `Сделай прогноз labor_h на 4 месяца`.
- Ожидаемо: вызов `forecast_metric` с метрикой `labor_h`, артефакты построены.

4. **Прогноз в фильтре класса**
- Установить фильтр класса в UI (например, `Насосы`).
- Вопрос: `Спрогнозируй затраты на 3 месяца`.
- Ожидаемо: прогноз построен на срезе выбранного класса, в тексте есть пометка про срез.

5. **Горизонт по умолчанию**
- Вопрос: `Спрогнозируй затраты ТОИР`.
- Ожидаемо: агент использует горизонт 3 месяца и явно пишет это в ответе.

6. **Проверка отказоустойчивости**
- Временно сломать Python-окружение (или недоступен `statsmodels`).
- Вопрос: `Спрогнозируй затраты на 3 месяца`.
- Ожидаемо: управляемый fallback (без падения `/api/agent`), trace заполнен.

## Что проверить в trace

- `trace.forecastModel` заполнен;
- `trace.horizon` соответствует запросу;
- `trace.forecastMetric` заполнен;
- `trace.forecastLatencyMs` > 0.
